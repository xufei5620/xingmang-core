# Retired independent production topology; use deploy/unified/compose.json and sources.json.
services: {}
retired-independent-topology: Independent deployment is retired. Use the monorepo scripts/unified-service.py check/build. Production cutover is not validated or authorized.
