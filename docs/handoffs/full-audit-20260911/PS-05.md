# PS-05 — check release output ancestors before writes or reads

- Status: fixed; branch `ai/codex/XM-FULL-AUDIT-boundaries-20260911`.
- Base for this finding: `9828928d678fdfb6e5856811be68090e2c8a475b` (F1 is a separate preceding commit).
- Problem: a normal release leaf beneath a parent junction passed lexical containment and leaf-only checks; the producer created output outside its project.
- Change: `Assert-NoReleasePathReparsePoints` walks the complete existing ancestor chain, including when the output leaf or several intermediate directories are absent. Only missing paths are tolerated; other filesystem lookup failures propagate. Producer calls it before directory creation; verifier calls it before artifact reads; `Assert-NoReleaseReparsePoints` calls it before traversal, protecting checksum writers/readers too. Existing lexical containment, leaf-link and nonempty-output checks remain.
- Changed files: release-image-gate library, producer, artifact verifier, new `test-release-path-boundaries.ps1`, this handoff.

## Targeted verification

Evidence: `G:\xingmang\logs\full-audit-20260910\phase2\boundaries\PS-05`.
Command: `pwsh -NoProfile -File invoice/scripts/test-release-path-boundaries.ps1 -FixtureRoot G:\xingmang\logs\full-audit-20260910\phase2\boundaries\PS-05\fixtures`.

| Run | UTC start | UTC end | Exit | Result |
| --- | --- | --- | --- | --- |
| RED | 2026-09-10T17:55:16.728551Z | 2026-09-10T17:55:17.565810Z | 1 | Seven intended ancestor-junction failures; producer evidence records writes outside the synthetic project |
| GREEN | 2026-09-10T17:55:54.123914Z | 2026-09-10T17:55:55.124539Z | 0 | 14/14 normal, leaf, parent, grandparent, absent-intermediate and nonempty controls |
| Restored GREEN | 2026-09-10T17:57:54.090604Z | 2026-09-10T17:57:54.867768Z | 0 | 14/14; exact fixed source bytes match GREEN |

`mutation-matrix.json` contains 20 executed copied-source mutations and their commands, UTC intervals, durations, source SHA256 values and exact failure patterns. Each of the 14 cases detects a matching behavioral mutation. Additional mutations remove output creation, disconnect producer/verifier calls, or move the producer guard after writes for all three ancestor shapes. All 20 fail at the intended assertion; late guards specifically fail because `outsideChanged=True` despite eventual rejection.

## Preservation and limits

- Test operations are restricted to newly created, retained Windows fixtures. They exercise the actual helper and exact contiguous producer/verifier directory blocks, stopping before locks, Docker and artifact/secret reads. No whole image build, deployment wrapper or full project gate was run.
- Every producer negative requires rejection before outside fixture entries change. The nonempty control requires its original marker and sole entry to survive intact.
- Existing release identities, tags, pins, environment configuration, images and lifecycle order are unchanged. No production/server/SSH/upstream/CPA/key access, remote writes, deletion of existing files, or Git history cleanup.
- Windows junction behavior was measured. Concurrent filesystem replacement and Linux filesystems were not tested. Full invoice/platform gate execution belongs to the parent integration task. The targeted test is an explicit entry; this worker did not edit shared full-gate runners.
