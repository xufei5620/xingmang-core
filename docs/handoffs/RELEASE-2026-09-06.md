# 发版材料：2026-09-06 合并发布（平台线 + 开票线）

- **平台线** `release/v0.1-launch` = `d16dbf1`（14 片，65 个文件）
- **开票线** `ai/claude/XM-INV-AUTOLOGIN` = `aca9037`（6 片，41 个文件）
- **状态：已合并、门禁全绿、未推远端、未构建镜像、未上生产。**

产品负责人 2026-09-06 指示：「准备发版材料，还有前面之前做完的累计的全部这一次发」。

---

## 一、先看这三件事

**1. 本次带两个数据库迁移，都在开票线，且 0029 不可逆。**

| 迁移 | 内容 | 可逆性 |
|---|---|---|
| `0028_notice_webhook_setting.sql` | 新建单例表存企业微信通知地址（密文） | 可逆（DROP TABLE） |
| `0029_newapi_auto_verification.sql` | **DROP 双人复核触发器与函数** + 回填历史待核验批次 | **不可逆** |

平台线**无迁移**。

**2. 0029 移除一道财务控制，是产品负责人明确决定的。**
移除的是 `newapi_verified_dual_control_guard`（要求 `proposed_by <> approved_by`）。
决定依据与三条备选路径见 `发票/docs/handoffs/XM-INV-NEWAPI-AUTOVERIFY.md`。
**回滚它需要先冻结所有已核验的 New API 批次**，再重建 0006 的触发器与函数——
0006 自带的那道 `DO $$` 检查就是为此而写。

**3. 生产上线后群消息形态会变。** 信封（XM-NOTIFY-ENVELOPE）在本次范围内，
所以告警消息会从现在的纯文本变成带 `【星芒·告警】` 徽标、编号与"该去哪处理"
的形态。运营会立刻察觉，这是预期的。

---

## 二、平台线：14 片（无迁移）

按合并顺序：

| 片 | 一句话 | 用户可见 |
|---|---|---|
| XM-USERS-HONEST0 | 用户管理页只显示有依据的字段 | 是 |
| XM-ALERTS-TAB-DURATION | 告警页签补「持续」列与时间窗，筛选进 URL | 是 |
| XM-PAY-STATUS-ROLLUP | 充值订单按状态汇总（整个查询区间，不随翻页变） | 是 |
| XM-SUB2API-0_2-MATRIX | 兼容矩阵声明 Sub2API 0.2 | 否（但采集判据从此为真） |
| XM-CR0006-WINDOW-EVIDENCE | CR-0006 观察窗口的只读取证脚本 | 否 |
| XM-CREDS-TAB-REFS + PROBE | 连接与凭据页签：凭据引用做实 + 探测历史与上游版本 | 是 |
| XM-WORKBENCH-JOBS + TRUNCATION + ALERTS-LIST-TRUNCATED | 工作台失败任务接真数据；两处静默截断改为说出来（截断判据由服务端给） | 是 |
| XM-NOTIFY-ENVELOPE + UPSTREAM-VERSION-ALERT + CATALOG + INTEGRATION | 消息信封统一、R6 上游版本变化告警、消息目录、接入规范与模板 | 是（消息形态变） |

**唯一的后端行为变更**：`/api/v1/alerts` 响应多两个字段（`limit`、`truncated`），
**只增不改**，老前端忽略即可。

---

## 三、开票线：6 片（两个迁移）

| 片 | 一句话 | 备注 |
|---|---|---|
| XM-INV-NEGATIVE-DEFICIT | 负余额带上金额；可解释的透支算已对账（产品负责人选的 B+C） | **需先跑两次 `install-economic`** |
| XM-INV-SUBMIT-NOTICE | 用户提交开票 → 推企业微信 | 默认关，需配地址 |
| XM-INV-RC100-HARDENING | 关掉 09-06 那次 27 分钟采集中断的两个成因 | 纯部署面 |
| XM-INV-NOTICE-VIEW / UI | 管理端能查「那条通知发出去了没有」（接口 + 申请详情一格） | |
| XM-INV-NOTICE-WEBHOOK-SETTING | 通知地址改为管理端设置页配置 | **迁移 0028** |
| XM-INV-NEWAPI-AUTOVERIFY | New API 充值按证据自动核验 | **迁移 0029，不可逆** |

---

## 四、已经跑过的门禁（本地，合并后的最终状态）

**平台线** `d16dbf1`：
- `go test -p 1 -count=1 ./...` 全绿
- `pnpm -r typecheck` / `test`（**1632 条**）/ `build` 全过
- `bash scripts/check-governance.sh` 退出 0
- `gitleaks detect --log-opts=acb7cd0^..HEAD` 无泄漏

**开票线** `aca9037`：
- `go test -p 1 -count=1 ./...` 全绿（带 `INVOICE_TEST_DATABASE_URL`）
- 前端 `tsc --noEmit` / `vitest`（**196 条**）/ `build` 全过
- `gitleaks detect --log-opts=30f0417..HEAD` 无泄漏
- 两个迁移在真实 PostgreSQL 上 `migrate.Up` 通过

**合并过程中修掉的一处**：`XM-NOTIFY-CATALOG` 的交接文档里原样写了那个点分
指标键，被 `generic-api-key` 判成泄漏——**那篇文档正是在讲这个误报**。因为门禁
扫的是增量提交范围，必须改在历史里；分支未推过，已 rebase 重写。

---

## 五、还没做的（发版必须做的事）

**这份材料只到"代码就绪"。以下每一项都还没做：**

1. **推送到 server**（两条线都只在本地；GitHub 那条「先别推」未撤）
2. **开票线的 RC 两段门禁**：源提交与签名 tag → 镜像构建、零发现扫描、SBOM、
   签名、独立校验 → 传输 → 部署 → 金丝雀 → 30 分钟盯守
   （见 `发票/docs/PRODUCTION-RUNBOOK.md` §3）
3. **平台线的部署**
4. **备份**：按惯例每次 roll-forward 前打一份

---

## 六、上线前必须由产品负责人执行/确认的

| # | 事项 | 为什么卡在这里 |
|---|---|---|
| 1 | **跑两次 `install-economic`**（sub2api、newapi） | B+C 那一片的前置，不跑发不了 |
| 2 | **确认 SMTP 在生产能真的发信** | 发票靠邮件送达；设置页点「发送测试邮件」即可 |
| 3 | **配企业微信通知地址** | 发版后在管理端设置页粘贴 → 保存 → 发送测试消息 |
| 4 | **历史消耗回填的效果** | 0029 只回填了核验状态与金额上限；可开票额取决于已消耗现金，历史消耗要另跑资格重投影（`--reproject-all`）。**建议先在恢复副本上验一次** |
| 5 | **NewAPI 退款流程** | runbook 新增 §9b：手动退款必须冻结批次或调低上限。**没有自动化，没有告警** |
| 6 | NewAPI 库建 `xm_readonly` 只读角色 | 平台侧资金对账/利润桥仍卡在它（不阻塞本次发版） |

---

## 七、回滚

- **平台线**：无迁移，回退镜像即可。
- **开票线**：`0028` 可逆；**`0029` 不可逆**——回滚前必须先把所有 `verified` 的
  New API 资金批次冻结，否则重建 0006 的触发器会因为存在"单人核验"的行而失败
  （那道 `DO $$` 检查会 RAISE）。
- 按 runbook §12：应用回滚必须保留一套匹配的开票库、十个源状态、两对 cutover、
  密钥与兼容镜像；只换回旧镜像或单独恢复某一个组件**不是**有效回滚。

---

## 八、盯守时重点看什么

1. **readyz / healthz** 与容器数（老三样）
2. **企业微信群**：消息形态是否变成带徽标与编号；有没有异常刷屏
3. **New API 用户的可开票额**：0029 生效后应当从 0 变成有值；如果仍是 0，
   多半是历史消耗没回填（见上面第 5 节第 4 项）
4. **`ingest-proxy` 日志无 `connect() failed`**（RC100-HARDENING 修的就是这个，
   上次那 27 分钟里 readyz 一度还是 200）
