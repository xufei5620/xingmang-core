# Evidence capture: sub2api real-sample probe

Generated by `cmd/evidence-capture capture` at 2026-09-02T05:47:23Z.

This directory is raw material for a product+security review under `docs/superpowers/specs/2026-08-28-platform-user-read-v2-design.md` §0/§10. **It is not an approval by itself.** A human reviewer must still read this directory and sign `docs/approvals/SUB2_REAL_APPROVAL.md` (SUB2_REAL_APPROVAL) before any real sub2api reader can be implemented. See `docs/runbooks/USERS-REAL-APPROVAL.md`.

## What was requested

Every request this run could ever make is fixed in `cmd/evidence-capture`'s source (`planFor` in `plan.go`) - there is no flag that lets a caller supply an arbitrary path. Only `GET`/read requests; see `mustNotBeMockRoute` for the routes this tool refuses to call even by mistake.
| purpose | method | path | status | latency (ms) | outcome |
|---|---|---|---|---|---|
| version | GET | `GET /api/v1/admin/system/version` | 200 | 341 | ok |
| users_page | GET | `GET /api/v1/admin/users?page=1&page_size=5` | 200 | 102 | ok |

## Upstream instance

- endpoint host: `api.solov.cc`
- target allowlist: `api.solov.cc`
- credential ref: `secret://sub2api-prod/read-token` (reference only - the plaintext token was resolved in memory for the duration of these requests and never written to any file, log line, or error message this tool produces)
- environment label: `production`
- upstream version string: `0.1.184`

## Fields captured (redacted) vs. dropped

- kept (redacted, present in `users_page.redacted.json` / `user_detail.redacted.json`): `balance`, `email`, `id`, `last_active_at`, `status`, `username`
- observed on the wire but **not captured** (name only - no value from these fields ever left this process): `allowed_groups`, `balance_notify_enabled`, `balance_notify_extra_emails`, `balance_notify_threshold`, `balance_notify_threshold_type`, `concurrency`, `created_at`, `current_concurrency`, `frozen_balance`, `last_used_at`, `notes`, `restrict_public_groups`, `role`, `rpm_limit`, `total_recharged`, `updated_at`

Adding a dropped field to the redacted evidence requires adding it, by name, to that platform's `ItemPolicy` in `sub2api.go`/`newapi.go` with an explicit redaction rule - reviewed the same way as any other code change, not a flag on this tool.
## Redaction applied

- `email` -> `platformusers.MaskEmail` (the platform's own reviewed masking: local part reduced to 0-2 leading characters, domain kept in full)
- `id` -> one-way per-run pseudonym, `u_` + 16 hex chars of `sha256(salt || "id" || id)` - stable within this run only, not reversible without the salt below, and the salt is not reused across runs
- `username`/`display_name` -> one-way per-run pseudonym, `name_` + 12 hex chars, independent hash domain from `id` (an id and a name with the same raw text never produce the same pseudonym)
- `balance`/`quota` -> every digit zeroed (a bare JSON number's leading digit becomes 1, not 0, only because JSON forbids a leading zero on a multi-digit number - every other digit is 0); sign, decimal point position and whether the upstream sent a JSON string or a bare JSON number are all preserved, so the *shape* evidence Task 4/5 needs survives while the real account balance does not
- `status`, `last_active_at`/`last_login_at`, NewAPI's `DeletedAt` -> passed through unchanged (small known value spaces / soft-delete markers, not PII)

capture salt for this run (not a credential; do not reuse across runs): `ace49d9ec1369a5c4765be4836a65aef`

## Files and sha256 (also in `SHA256SUMS`)

| file | sha256 |
|---|---|
| `request-log.json` | `ee69501c8240c1a1a10b7c5e795dc8c57d7919c6de32708fd7c934f0ca183f2e` |
| `version.json` | `8f3991ad282def8b5774479c57731a6318a19fc8d6399f42d08dd8e36d1da7d8` |
| `users_page.redacted.json` | `02013b4f7761cbd60e7aea86bbd6ca8ff4fd3471a9c61c3307b88ada3e7ce4ee` |
| `user_detail.redacted.json` | `d250d91f403b4dbafbaf8f1b9c841b8ae742734f4e3774167248e42f08ddaad9` |

## Feeds into

- `docs/evidence/EV-2026-08-28-platformusers-sub2api-v2-shape.md` - the narrative evidence document `docs/superpowers/plans/2026-08-28-platform-user-read-v2.md` Task 4/5 requires; a human writes its prose citing this directory's path and the sha256 sums above.
- `connectors/platformusers/testdata/sub2api/users_page.redacted.json` <- copy this run's `users_page.redacted.json` verbatim (same envelope shape).
- `connectors/platformusers/testdata/sub2api/user_detail.redacted.json` <- copy this run's `user_detail.redacted.json` verbatim.
- `docs/approvals/SUB2_REAL_APPROVAL.md` (blank signature lines for 产品 and 安全) - the approval text itself; this directory's path is what goes in its evidence-path fields.
