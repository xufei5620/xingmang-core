# 被管理系统动态清单（Inventory：描述"现在是什么"；随快照更新，不写入 ADR 正文）
systems:
  - id: sub2api-prod
    type: sub2api
    endpoint: https://api.solov.cc
    environment: production
    observed_at: 2026-08-26T00:00:00Z
    # detected_version 只填**探测**出来的值。这个实例至今没跑过 Version()，
    # 所以它仍是「未探测」——把下面那个源码核对版本抄上来就是把一次代码阅读
    # 伪装成一次观测，本清单描述的是「现在是什么」，不是「大概是什么」。
    detected_version: "未探测（XM-0017 已交付只读客户端，等待真实凭据后由 Version() 回填）"
    # 客户端当前逐字段核对过的上游源码版本（不是本实例的运行版本）。
    # 0.1.183 = K:/sub2api-src @ efb46db，见 EV-2026-08-27-sub2api-read-survey.md；
    # 0.2.1  = K:/sub2api-src @ v0.2.1，2026-09-06 逐条核对了本客户端调的七条
    #          路由与它们的字段（见 connectors/sub2api/client.go 的矩阵注释）；
    # 客户端同时兼容 0.1.133/0.1.183 两版的支付金额形状（XM-R013），0.2.1 的
    # 支付看板仍是 0.1.183 起的币种 map，没有再变。
    client_verified_against: "0.1.183 + 0.2.1（源码核对，非实例探测）"
    # 与 detected_version 分开记：这不是探测值，是 2026-09-06 在部署主机上看
    # 容器内文件得到的——`/app/sub2api` 二进制 mtime 2026-09-05 17:45 CST，
    # 版本 0.2.1（后台"点击更新"就地替换二进制，镜像标签不跟着变）。够用来
    # 决定兼容矩阵该声明什么，不够格填进 detected_version。
    observed_binary_version: "0.2.1（宿主机文件观测，非 Version() 探测）"
    user_count: 2482
    source_watermark: "未采集"
    # 拿到凭据后开采之前必须做的两件事，详见 contracts/connectors/sub2api.read.v1.md
    # 的「接入前置」：① 对采集账号 POST /api/v1/admin/compliance/accept，
    # 否则 admin 组全部 GET 返 423；② 5 个 mock 端点不得采集。

  - id: newapi-prod
    type: newapi
    endpoint: https://xm.solov.cc
    environment: production
    observed_at: 2026-08-26T00:00:00Z
    detected_version: "v1.0.0-rc.25"
    user_count: 27
