# 第二轮需求挖掘(非 Issues)· 平台侧整理与处置(2026-08-28)

> 历史源报告：原独立平台工作区 `星芒统一控制平台` 下的相对目录 `中转站管理需求持续挖掘/`，
> 星芒中转站管理需求挖掘报告_第二轮_非Issues_2026-08-28.html(结构化数据
> artifact_round2_nonissues.json)。本文只做**处置**:每条归入哪期、与现状
> 的重叠度、执行门槛。细节以源报告为准。
> 此处仅记录历史出处，未核验当前副本；当前工作区见 monorepo 根目录的
> `docs/MONOREPO-MIGRATION.md` 和 `docs/handoffs/MONOREPO-CUTOVER.md`。

## 一、总评

- 17 条新需求(P0×4 / P1×11 / P2×2),证据等级 A/B 为主,可采信;
- **10 条否决信号与平台红线完全一致**(不碰第三方会话/不直连上游库/不进实时
  流量路径/不默认存 Prompt 正文/不做抗封与自动注册)——采纳其否决结论,
  任何后续任务不得复活这些方向;
- 12 条证据增强是对第一轮结论的修正,多数落在既有线的验收标准加严上
  (告警带证据链接与测试消息、升级门禁带受影响版本映射、配额带代次与置信度等),
  逐条并入对应线的 follow-up。

## 二、17 条处置表(期别沿用报告的四期闸门)

| 期 | 条目 | 处置 | 与现状 |
|---|---|---|---|
| **P0 闸门期** | R2-10 多副本任务唯一性 | 纳入,集群租约+重复探测 | 部分有(River 唯一周期任务),缺集群级租约 |
| | R2-13 CPA 管理面网络隔离 | 纳入,**M4 CPA 接入的前置门** | 未开始;M4 设计必须先过它 |
| | R2-14 CPA 插件供应链准入 | 纳入,同为 M4 前置门 | 未开始 |
| | R2-15 Connector 采集预算/源站保护 | 纳入,现有连接器加 cost class/自适应轮询/backoff | 部分有(只读闸/超时/5min);缺预算声明 |
| **P1-A 可信运营** | R2-05 用户旅程 SLO(合成探测) | 纳入,新监测线(canary 身份低频) | 全新 |
| | R2-06 客户可见信任中心 | 纳入,**独立于员工后台的新产品面** | 全新 |
| | R2-08 模型真实性/构建来源 | 并入 **M1.5 渠道保障**范围 | 已规划,范围扩充 |
| | R2-16 上游公告/调价聚合流 | 纳入,新只读连接器→运行保障页 | 全新 |
| | R2-17 实例接入向导/配置诊断 | 纳入,资源目录登记流增强 | 部分有(登记 Action) |
| **P1-B 经营集成** | R2-01 迁移兼容窗口 | 纳入,事件驱动(发生迁移时执行) | 工具型,按需 |
| | R2-02 成本中心归因 | 并入成本线扩展(platform_id 四桶→成本中心映射) | 台账已有归因列,缺映射面 |
| | R2-03 下游 usage 结算事件 | 纳入,新集成面(签名/幂等/死信) | 全新 |
| | R2-07 支付适配器目录 | 并入 **M3 支付**范围 | 已规划,范围扩充 |
| | R2-11 客户端接入包/配置发布 | 纳入,后置(companion 模式) | 全新 |
| | R2-12 租户自助用量门户 | 纳入,后置(delegated key 投影) | 全新 |
| **P2 体验期** | R2-04 定时价格活动 | 纳入,**硬依赖 Foundation-B**(写+审批) | F-B 后 |
| | R2-09 PWA 应急运维 | 纳入,UI 线后置(移动默认只读) | 全新 |

## 三、执行纪律(报告四道门,全部采纳)

1. P0 期四条是**任何自动写/插件/高频采集的前置闸**;
2. 公开面一律脱敏;外部内容(公告/评论)是数据不是指令,不触发自动写;
3. 用户面与员工面权限分域,用户侧只接触 delegated key/指纹与自有数据;
4. 批处理先预览真实对象数、限并发、逐项结果、支持部分失败与补偿。

## 四、证据增强并入点(摘要)

告警线补:测试消息/证据链接/实例归属展示;升级线新增:受影响版本映射+对象
hash+隔离恢复门禁(接 XM-0050 的保留/审计基础);配额语义:Provider scope+
代次+置信度,旧代次不覆盖新证据;比价/价卡:采购价/销售价/估算/真实账单
分栏(接成本线);日志采集:collector ownership+队列窗口/RPO+缺口标记
(接 reqlog/CPA 线)。全文见源报告 evidence_enhancements 数据集。
