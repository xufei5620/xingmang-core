# v1.0 只读运营台冲刺——最终交付报告(2026-08-28)

> 用户授权自主冲刺期间完成。全部工作已合入 release/v0.1-launch 并在本机
> staging 栈(http://127.0.0.1:8088)实机验证。

## 一夜合入的 14 个 PR(全部 CI 4/4 绿)

| 线 | PR | 内容 |
|---|---|---|
| UI | #80 | 阶段1:令牌换向(靛蓝浅色+深侧栏)+Shell+Storybook 八态,顺修全站对比度 AA |
| 数据 | #81 | 请求详情:reqlog 只读网关(fake),高敏分级+查看审计+无导出 |
| 成本 a | #82 | 登记簿+计量取数连接器;money 包 big.Int 防回绕 |
| 文档 | #83 | 原型严格对齐审计:ADMIN-IA v3+12 片切片计划 |
| 成本 b | #84 | profit_daily 台账+5min 采集;三条不静默纪律;毛利=生成列 |
| UI | #85 | 第1片:侧栏四分组、平台 7→4、11 条 redirect、404 兜底 |
| 数据 | #86 | NewAPI 真实客户端:第五道闸(伪GET写端点黑名单含 token 自轮换陷阱) |
| UI | #87 | 第2片:运营工作台(无合成健康分)+Ctrl K 全局搜索(仅导航) |
| 成本 | #88 | NewAPI 收入只读直查(DSN 通道) |
| 成本 c | #89 | 订阅批次+代理资产+摊销+损失科目(SoloAI 没有的新口径) |
| UI | #90 | 第3片:DataTableV2+PageState 八态 |
| 成本 e | #91 | 影子对比工具(平台 vs SoloAI,分粒度,仅计量型) |
| UI | #92 | 第4片:用户管理+渠道保障蓝图+财务五子签框架 |
| 成本 d | #93 | balance_history+runway+看板毛利卡+演示种子 |
| UI | #94 | 第6片:服务器七页签+治理段六页+扩展蓝图 |
| UI | #95 | 第5片:上游管理登记簿 UI(四写 Action)+渠道管理收窄 |
| 收尾 | #96 | group_rate 列+R5 runway 告警+阈值单点解析 |
| 加固 | #97 | R009 审计规范转义漏洞修复(真漏洞)+R011 限流+R012 保留策略 |

## 终局实测(Playwright,staging)

- 经上游管理**表单**登记令牌映射与平台归属、经 **Action API** 登记订阅批次
  ($200/30天),全部产生 run_id 并进哈希链审计(审计页可检索);
- 采集流水线自动产出:**本期消耗 $3.88、整体毛利 $8.47**,覆盖不全时
  如实标注 1/2 而不给假合计;
- 全站走查:工作台/四平台九页签/服务器七页签/治理段/请求详情/告警/审计全部
  按原型渲染态就位,未实装格全部诚实占位注明里程碑。

## 等用户的三件套(回来后做)

1. **Sub2API endpoint+key**(SWITCH-SUB2API-REAL.md 三个值)→ 切真实数据,
   影子对比 14 天倒计时开始(工具已就绪:cmd/platform-shadow);
2. **reqlog 源码/端点清单**(/root/reqlog)→ 请求详情切真(契约 DRAFT 待核对);
3. **合并 #43**(release→main,一键)。

## 已知缺口(不阻塞,按优先级)

- 订阅批次/代理资产的**登记表单 UI** 未建(展示有;登记走 Action API 可用,
  scope `finance.subscription.manage` 未进开发态默认清单);
- runway 需隔日出数(窗口刻意不含今天);sub2api 余额真实解析待
  /admin/accounts 响应样本;newapi 余额需上游开 CHANNEL_UPDATE_FREQUENCY;
- 订阅渠道收入侧未知→毛利合计卡持续「数据不完整」,需订阅账号↔自营账号
  映射口径(037c 记录的多自营拆分问题);
- 应用库账号是 superuser(REVOKE 空承诺,#97 已立证据脚本),真修需拆角色;
- F-B 写操作体系(XM-0030 设计待拍板)、M1.5/M2/M3/M4 平台接入按里程碑。
