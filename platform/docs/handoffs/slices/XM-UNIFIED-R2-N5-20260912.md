# XM-UNIFIED-R2-N5-20260912

R2 N-5 与负责人新增的 D 就绪预算要求。基于 `c3d8b16dd272603013b56c9a7672ef6759684dbf`，只修改操作器、相关验证和说明，不修改开票 11 闩或 handler 的业务行为。

`start_new` 之前开始一个不可重置的 300 秒 monotonic 预算。两项目启动和健康等待、运行清单、实际 HTTP 观察均消费同一剩余时限。Compose CLI 与只读 HTTP 子进程使用真实进程超时，避免只在事后计时或慢速 HTTP 逐字节延长 socket 超时。第一次观察 HTTP 200、三模块 ready、invoice_ready 与原始 invoice.ready 全部为真后保存 UTC/monotonic 耗时；原始公开响应字节和 SHA 保留在每次操作目录。

D 逾期会非零、清理副本/临时身份，保留 `candidate-readiness.json`、`rehearsal-result.json` 和 `STOPPED.md`，不发放 PASS 收据。预算终止后解除对恢复命令的约束，E 失败仍走原自动回滚。C1/C2 的服务器实际普查与部署批准由本轮负责人流程处理，此切片未访问 Docker 或服务器。

N-5 报告的“not_evaluated 完全不可达”前提不成立：`source_health_query` 等分支只确定来源模块失败，投影仍是 not_evaluated；`eligibility_health_query` 等分支反之。真实 handler 回归和 Go overlay 变异证明这两条路径，手册改为解释其含义并继续拒绝未就绪状态。

证据目录：`G:/xingmang/logs/unified-review-fixes-r2-20260912/N-5/`。这是本地验证，不是服务器 D/E 结果。

| 验证 | UTC 起止 | 原始退出码 |
|---|---|---:|
| RED：两项目独立 300 秒，缺总预算记录 | 06:46:14.265927Z → 06:46:15.461365Z | 1 |
| 首轮预算测试 GREEN | 06:48:04.500110Z → 06:48:06.341777Z | 0 |
| operator 全部 182 测试 | 06:50:37.434735Z → 06:50:44.459472Z | 0 |
| invoicehost `go test -count=1 -p=1` | 06:51:58.065765Z → 06:52:19.508013Z | 0 |
| 7 个隔离 Python 变异 | 06:52:54.351336Z → 06:53:02.952949Z | 每个 1 |
| Go overlay 短路可达性变异 | 06:53:43.006085Z → 06:53:44.264263Z | 1 |
| 恢复后 operator 全部 182 测试 | 06:53:57.517891Z → 06:54:04.803688Z | 0 |
| 恢复后 invoicehost `-count=1` | 06:54:12.245045Z → 06:54:13.235771Z | 0 |

以上日期均为 2026-09-12。`01-RED` 的行为断言显示实际 `[300, 300]`，应为消耗启动时间后的 `[180, 120]`；其他初始失败对应尚不存在的预算记录/常量。7 个 Python 变异分别移除共享 Compose 预算、进程硬超时、check 阶段不可重置约束、原始开票闩报告校验、恢复命令预算解除、STOPPED 输出和慢速 HTTP 硬超时。全部命中断言；原工作树未被变异写入。Go overlay 把短路未知模块初值改为 not_ready 后，两条实际 handler 路径均红；去掉 overlay 后恢复绿。

风险/限制：300 秒只覆盖新运行服务开始启动到首次全绿观察，前面的备份恢复、冻结数据校验和迁移单独计时；不把它描述为整个 D/E 的总时长。超时杀死的是本次 CLI/探测进程，已创建的容器继续由原受归属约束的 D 清理/E 回滚处理。生产冷启动耗时尚需服务器 D 实测，未通过前不能切换。
