{
  "id": "publishing.asset.remove",
  "version": "1",
  "risk_level": "L1",
  "permission": "publishing.manage",
  "principal_types": [
    "HUMAN"
  ],
  "environments": [
    "development",
    "staging",
    "production"
  ],
  "idempotency": "IDEMPOTENT_BY_ASSET_ID",
  "write_confirmation": "ACTION_RESULT_AND_QUERY_REFETCH",
  "approval": {
    "required": false
  },
  "compensation_mode": "MANUAL",
  "params": {
    "asset_id": {
      "type": "string",
      "required": true
    }
  },
  "notes": "仍被草稿引用的素材删不掉（库层 ON DELETE RESTRICT，映射成 CONFLICT）。 XM-EXT-PUBLISHING（ADMIN-IA §5.4.1，2026-09-08 裁定变更）。平台今天没有任何出站投递器：内容发布的第一层只保证「内容的生命周期在平台内是真的」。"
}
