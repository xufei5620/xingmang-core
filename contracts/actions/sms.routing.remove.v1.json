{
  "id": "sms.routing.remove",
  "version": "1",
  "risk_level": "L1",
  "permission": "sms.manage",
  "principal_types": [
    "HUMAN"
  ],
  "environments": [
    "development",
    "staging",
    "production"
  ],
  "idempotency": "NOT_REQUIRED",
  "write_confirmation": "ACTION_RESULT_AND_QUERY_REFETCH",
  "approval": {
    "required": false
  },
  "compensation_mode": "REVERSIBLE",
  "params": {
    "rule_id": {
      "type": "string",
      "required": true
    }
  },
  "notes": "XM-SMS2（ADR-022 决策 3）删一条路由规则。规则不存在（含已被别人删掉、或 rule_id 不是 uuid）回 PRECONDITION_FAILED 而不是 INTERNAL：页面要能告诉人「已经没了，刷新一下」。删掉后该「服务 × 国家」的要号回落到更粗的规则或装配顺序，不会没有供应商可用。compensation_mode REVERSIBLE：再用 sms.routing.set 设一条同键的就回来了（rule_id 会变，页面不该缓存它）。"
}
